This `static` directory is served at `/static` by FastAPI.

Place CSS, JS, images (e.g. default_avatar.png) and other static assets here.

The application expects a `default_avatar.png` by default — add one if you need avatars to display correctly.
